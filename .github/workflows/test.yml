name: Test App

on:
  workflow_run:
    workflows: ["Build App"]
    types:
      - completed

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: sampleapp-build
          path: ./publish

      - name: Run Tests
        run: |
          echo "Running tests..."
          # Replace with your real dotnet test command if you have tests
          echo "No tests found, skipping."

name: Test App
on:
  workflow_run:
    workflows: ["Build App"]
    types:
      - completed
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: dotnet test SampleApp/SampleApp.csproj
